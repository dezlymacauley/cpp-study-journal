/*

! means not
&& means and
|| means or

NOTE: not has higher precedence than and
and has higher precedence than or

NOTE: `not` is a unary operator
and and or are binary operators

NOTE: Short-Circuit Evaluation
When evaluation a logical expression, C++ stops as soon as the result is known.

===============================================================================

NOTE: Compound Assignment Operators

+=          `a += 1;` is the same as `a = a + 1;` 
-=          `a -= 1;` is the same as `a = a - 1;`
/=          `a /= 5;` is the same as `a = a / 5;`
*=          `a *= b + c` is the same as `a = a * (b + c)`
%=
>>=
<<=
&=
^=
|=

cost += items * tax; same as `cost = cost + (items * tax);`

===============================================================================

*/
